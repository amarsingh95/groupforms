import { ImageUrlPipePipe } from './image-url-pipe.pipe';

describe('ImageUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
